# 12:55 — 전사 실시간 OX 서바이벌

매주 월요일 점심시간 마지막 5분, 전 직원이 폰으로 O·X 서바이벌을 치른다.
KIPI Playground(한국특허정보원 e스포츠 공모전 / 팀 KIPlay) 출품작.

## 실행

```
node server.js                  # 게임. 의존성 0, Node 18+
npm test                        # scripts/e2e.js — 실제 플레이 검증 (56항목)
node tools/quizgen/server.js    # 문항 파이프라인 (로컬 전용, :8899)
```

- 참여자 `/` · 전광판 `/board.html` · 운영자 `/admin.html` (키 기본값 kipi) · 게임설명 `/spec.html`
- 화면 픽스처: `/?screen=lobby|question|reveal|revive|sudden|champion`, `/board.html?scene=ground|rooftop`
- 배포: git push → Render 자동 배포 (render.yaml). ADMIN_KEY는 대시보드에서 설정.

## 구조

```
server.js         모든 게임 로직. SSE(/api/stream, /api/spectate) + POST. 단일 프로세스, 스케일아웃 안 함.
data/questions.json   문제 은행 (사람이 작성). evidence 필드가 없는 문항은 파이프라인에서 폐기 대상.
data/employees.json   명부·부서·본부색. 사번 형식은 idFormat으로 조정.
public/crowd.js   군중 무대 렌더러. 게임장(도1)·옥상(도2) 두 장면. 그리기만 하고 판정은 안 한다.
public/ending.js  우승 공보 카드. crowd.js의 팔레트·도장·해칭을 가져다 쓴다.
public/music.js   배경음악 (audio/*.wav). 로고송 19초 · 우승곡 102초.
public/sfx.js     신스 효과음 + TTS 나레이션. estimate()가 문장 읽는 시간을 추정.
public/commentary.js  상태 변화 → 중계 멘트. 숫자는 서수로("두 번째 문항") — TTS가 "2번"을 "이번"으로 읽는다.
public/roofimage.js   옥상 사진 파이프라인 (선택). roof-lab.html에서 난간선을 맞춘다.
```

## 아트 디렉션 — 등록특허공보

실제 공보 두 건(10-2231365 KIPI 소유, 10-3006496 지식재산처 개편 후)을 조판 근거로 삼았다.

**절대 규칙: 인주(#B03A2E)는 판정에만 쓴다.** 문항 중 화면에 빨강이 0%여야 하고,
정답 공개 순간에만 등장한다(등록 도장 登 / 거절 사선). 그 색을 보는 것이 곧 결과다.
다른 용도(강조·경고·장식)에 빨강을 쓰면 이 장치가 죽는다. 예외는 서든데스의 「숫자 입력」 안내뿐.

- 먹(#141414)과 종이(#FAF7F0). 면은 해칭, 명암·그라디언트·광택·드롭섀도 금지.
- 본문·제목은 명조(--serif), 라벨은 고딕, 숫자·부호는 모노. INID 괄호 숫자가 디자인의 서명.
- 캔버스는 alpha:true — 불투명이면 크롬이 글자에 LCD 서브픽셀 AA를 써서 색테가 생긴다.
- 대비 기준 4.5:1 (CSS 상단 CONSTRAINTS CARD "3m 거리 판독"이 원조).

## 게임 규칙의 뼈대

- 게임장은 하나다. 층 상승 구조는 걷어냈다(커밋 44e61d4). 장면은 ground와 rooftop(우승 전용)뿐.
- O·X 구역 상자가 곧 버튼이다. crowd.js의 BOX 상수와 CSS .choice-o/.choice-x가 같은 비율을 써야 한다.
- 문항 브리핑(armMs 3400): 문항 공개 후 응답 시계가 켜지기까지 중계가 읽을 시간.
  서버가 armAt 전 답안을 409로 거절한다(250ms 유예). 값의 근거는 "난이도 어려움" 문장 낭독 시간.
- 서든데스 브리핑(suddenArmMs 2200): 뜸 → 우승곡과 함께 출제 → 20초 뒤 우승 확정이 곡과 겹친다.
- 생존자 30명 미만이면 서버가 집계를 끊는다(클라이언트 숨김이 아니라 서버 차단 — 개발자도구 방어).
- 회차 예산은 5분(점심시간 마지막 5분). e2e가 300초로 지킨다.

## 문제 은행 규약

- 두 갈래: 특허 상식(검증 완료) + 그 주의 사내 공지·IP뉴스 발췌.
- 발췌 문항의 장치: 원문 그대로면 O, 숫자·순위를 바꾸면 X. evidence에 반드시 원문을 남긴다.
- 화면에 혼자 떠도 판정 가능한 "진술문"이어야 한다. 조각(바이라인, 체크박스 항목)은 금지.
- 선발: pickQuestions()가 난이도 사다리 [쉬움,쉬움,보통,어려움,어려움]로 5문항. 서든은 6개 중 1개.
- 파일 교체 후 POST /api/admin/reload {key} — 재시작 불필요.
- 자동 생성: `node tools/quizgen/server.js` → http://127.0.0.1:8899 (수집→생성→검수→반영→자동 리로드).
  그룹웨어 비밀번호가 필요하므로 이 도구는 로컬 전용이다 — 배포 서버에 올리지 않는다.
  규칙 엔진에는 진술문 게이트(불릿·헤드라인 차단), 의역된 O 문항에는 검수 필수 표시가 붙는다.
- Anthropic API 키(LLM 엔진용)는 admin 콘솔 또는 스크래퍼 화면에서 저장한다 —
  tools/quizgen/output/config.json (gitignore). admin의 입력창도 브라우저→127.0.0.1:8899 직행이라
  키가 게임 서버(Render)에 닿지 않는다. GET/POST /api/config, 우선순위: 입력 키→저장 키→환경변수.

## 정기 회차와 음악

- **정기 회차는 매주 월요일 12:55 KST 자동 시작.** 12:50에 서버가 스스로 대기실을 열고
  (프리쇼 5분), 12:55 정각 1번 문항. 시각은 GAME_DOW/GAME_HOUR/GAME_MINUTE 환경변수,
  자동 개장은 AUTO_START=0으로 끈다. 대기 화면에는 "다음 게임은 O월 O일 (월) 12:55에
  시작됩니다" 안내가 뜬다(state.nextGameAt).
- 로고송: 접속하면 한 차례(1초 후 자동 시도, 차단되면 첫 터치에서). 개별 1분 반복은 없앴다.
- 프리쇼(12:50~12:55): 서버가 SSE 'jingle' 큐를 1분에 한 번 브로드캐스트하고, 접속한 모든
  폰·전광판이 같은 순간에 튼다 — 회장 전체가 하나의 스피커.
- 체험 대기실 = 로고송 남은 시간. 「체험 시작」을 누르면 곡의 잔여 초가 대기실 카운트다운이
  되고, 곡이 끝나는 순간 1번 문항이 나간다. 재생이 막히면 3초. (3/15/60초 옵션은 삭제)
- 우승곡: 서든데스 출제와 함께 시작(또는 서든 없으면 우승 확정 때). 우승 공보 카드는 스스로 걷히지
  않는다 — 곡이 끝나도 화면 유지(캡처용). 누르거나 다음 회차가 열려야 걷힌다.
- 실전 회차 진행 중(문항~결과) 폰은 음악을 틀지 않는다. 프리쇼 로고송과 체험 모드가 예외.
  회장 음향은 전광판 몫.

## 함정 (반복해서 당한 것들)

- **월요일 12:50~12:55 KST에는 e2e를 돌리지 말 것.** 자동 개장 티커가 회차를 열어 시나리오를
  덮는다. 그 시간대에 꼭 돌려야 하면 서버를 AUTO_START=0으로 띄운다.
- **e2e 인원 수 단언이 깨지면 십중팔구 외부 세션이다.** 브라우저 탭 하나가 SSE로 붙어 있으면
  참가자 수가 전부 어긋난다. e2e의 resetServer() 방화벽이 "e2e 밖 클라이언트" 경고를 낸다.
  실행 전 탭을 spec.html 같은 정적 문서로 치우고 sessionStorage를 비울 것.
- 시나리오 경계에서 소켓 close 이벤트가 늦으면 유령 참가자가 생긴다 — resetServer()가 흡수한다.
- 서버는 result 단계에도 상태를 계속 보낸다. 클라이언트 연출(옥상, 카드)이 setState에 덮이지 않게
  championIndex 가드가 있다. 픽스처(?scene=)는 sceneLocked로 잠근다.
- rAF는 배경 탭에서 멈춘다. 화면을 덮는 연출의 종료를 rAF에만 맡기지 말 것.
- 이름표를 사람 머리 위에 직접 그리면 겹친다. 도면부호 지시선(callout)으로 좌우 여백에 뺀다.

## 부호 용어

'원장' 대신 '스페셜 참가자'(vip 플래그 + title). 왕관 배지가 그 표식.
2년차 미만은 joinYear로 자동 판정되어 부활권 1개. 사번 테이블이 오면 employees.json roster에 반영.
